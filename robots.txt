# Directives for all web crawlers
User-agent: *

# Allow crawlers to access all public parts of the site.
Allow: /

# Disallow crawlers from accessing the admin panel.
Disallow: /admin/

# Location of the sitemap (replace with your actual sitemap URL).
Sitemap: https://DevSparksoft.com/sitemap.xml
